// TEMPORARY STUB (Task 1). The real nut-js backend is implemented in Phase 2 Task 2,
// which replaces this file. For now we re-export MockInputBackend so main.ts compiles
// and the build passes without wiring nut-js into the entry point.
export { MockInputBackend as NutInputBackend } from "./input-backend";
